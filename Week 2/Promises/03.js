function callback() {
  console.log("pogo");
}

setTimeout(callback, 3000);
// the set timeout promises to call the callback function after 3 seconds, this is the callback based approach
// we an use the promise based approach
