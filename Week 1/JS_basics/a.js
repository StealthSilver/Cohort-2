// Variables

let name = "John"; // Variables that can be reassigned
const age = 90; // constant variable, cannot be reassigned
let isAdult = "True"; // boolean variable, can be true or false

console.log(name);
console.log(age);
console.log(isAdult);

let color = "red";
let height = 190;
let pizza = true;

console.log(
  `My favourite color is ${color} , my height is ${height} and likePizza is ${pizza}`
);
