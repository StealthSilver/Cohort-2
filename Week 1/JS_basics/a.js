// Variables

let name = "John"; // Variables that can be reassigned
const age = 90; // constant variable, cannot be reassigned
let isAdult = "True";

console.log(name);
console.log(age);
console.log(isAdult);

let color = "red"; // string type variable
let height = 190; // integer type variable
let pizza = true; // boolean variable, can be true or false

console.log(
  `My favourite color is ${color} , my height is ${height}cm and likePizza is ${pizza}`
);
