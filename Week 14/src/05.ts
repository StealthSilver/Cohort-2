// explicit return

function nextSum(a: string, b: number): string {
    return a + b; // 'a' is a string, 'b' is a number, so the result is a string.
}

console.log(nextSum("3", 6)); // Output: "36"
